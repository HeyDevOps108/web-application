apiVersion: apps/v1
kind: Deployment
metadata:
  name: obcs-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: obcs-service
  template:
    metadata:
      labels:
        app: obcs-service
    spec:
      containers:
        - name: obcs-service
          image: __IMAGE_ABSOLUTE_PATH__
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: 100m
              memory: 100Mi
            requests:
              cpu: 50m
              memory: 50Mi