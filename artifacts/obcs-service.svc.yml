kind: Service
apiVersion: v1
metadata:
  name: obcs-service
  namespace: preprod
spec:
  selector:
    app: obcs-service
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP